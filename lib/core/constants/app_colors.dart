import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Branding Colors
  static const Color primary = Color(0xFF0F2C59); // Deep Sporty Blue
  static const Color secondary = Color(0xFFFF6B00); // Energetic Orange/Amber

  // Backgrounds
  static const Color backgroundLight = Color(0xFFF8FAFC);
  static const Color backgroundDark = Color(0xFF070B14);

  // Cards & Containers
  static const Color cardLight = Colors.white;
  static const Color cardDark = Color(0xFF0B1220);

  // Borders & Dividers
  static const Color borderLight = Color(0xE2E8F0FF);
  static const Color borderDark = Color(0xFF1E293B);

  // Text
  static const Color textPrimaryLight = Color(0xFF0F2C59);
  static const Color textPrimaryDark = Colors.white;
  static const Color textSecondary = Colors.grey;

  // Feedback Colors
  static const Color success = Color(0xFF16A34A);
  static const Color error = Color(0xFFDC2626);
  static const Color warning = Color(0xFFD97706);
}
